/**
 *  Crie variáveis tipadas para:
 *      Nome (string)
 *      Idade (number)
 *      Está ativo (boolean)
 *      Lista de números (array) 
 */

const nome: string = 'João';  // Tipo texto

const idade: number = 18;  // Tipo númerico

const ativo: boolean = true;  // Tipo boleano

const lista_de_numeros: number[] = [1, 2, 3, 4];  // Tipo lista
// ou
// const lista_de_numeros: Array<number> = [1, 2, 3, 4];  // Tipo lista

console.log(nome);
console.log(idade);
console.log(ativo);
console.log(lista_de_numeros);