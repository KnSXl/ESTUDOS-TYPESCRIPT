// Interface (Muito importante 🚀)
// Interface define o formato de um objeto.
interface Produto {
    nome: string;
    preco: number;
    disponivel: boolean;
}

let item: Produto = {
    nome: "Notebook",
    preco: 3000,
    disponivel: true
};